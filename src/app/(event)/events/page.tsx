'use client'
import React, { useState } from 'react';
import { PDFDownloadLink } from '@react-pdf/renderer';
import {PDF} from '@/components/pdf'; // Adjust the import path as necessary
import { stripe } from '@/lib/stripe';

const App = () => {
  return (
    <div>
      <h1>Generate PDF with QR Code</h1>
        
    </div>
  );
};

export default App;